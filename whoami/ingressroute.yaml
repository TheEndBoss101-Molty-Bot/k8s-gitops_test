apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: whoami
spec:
  entryPoints:
    - http-external
    - https-external
  routes:
  - match: Host(`whoami.local`)
    kind: Rule
    services:
    - name: whoami
      port: 80
  # For HTTPS test (uncomment when you have certs):
  # tls:
  #   certResolver: cloudflare   # once you enable the resolver above